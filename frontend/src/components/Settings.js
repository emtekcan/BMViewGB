import React from 'react';
import styled from '@emotion/styled';

const PageContainer = styled.div`
    padding: 20px;
    text-align: center;
`;

const Settings = () => {
    return (
        <PageContainer>
            <h1>Settings</h1>
            <p>This page is under construction. It will contain accessibility options and custom color scales.</p>
        </PageContainer>
    );
};

export default Settings; 