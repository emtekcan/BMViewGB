import React from 'react';
import styled from '@emotion/styled';

const PageContainer = styled.div`
    padding: 20px;
    text-align: center;
`;

const About = () => {
    return (
        <PageContainer>
            <h1>About</h1>
            <p>This page is under construction. It will provide a brief explanation of BMViewGB's functionality and list data sources.</p>
        </PageContainer>
    );
};

export default About; 